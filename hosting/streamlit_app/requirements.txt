# Dependencies for the Streamlit demo app.
#
# Streamlit Community Cloud reads this file and installs these on its server before
# running app.py. If a package the app needs is missing here, the deploy fails with
# a ModuleNotFoundError - so anything the app imports must be listed.
#
# Note what is NOT here: hl7lib. That is your own code, imported straight from
# build_from_scratch/ (see the sys.path line at the top of app.py), and it is pure
# standard-library Python - no dependencies of its own. Nothing to install.

streamlit>=1.30
Faker>=24.0
